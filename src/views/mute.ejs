<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mute Settings</title>
    <link rel="stylesheet" href="/CSS/styles.css"> <!-- Link to your CSS file -->
</head>
<body>
    <div class="container">
        <h1>Mute Settings</h1>
        <p class="status">Hello, <strong><%= user.username %></strong>!</p>
                <form action="/mute/<%= user.discordId %>" method="POST">
            <div class="radio-group">
                <label>
                    <input type="radio" name="mute" value="true" <%= user.mute ? "checked" : "" %> />
                    Mute
                </label>
            </div>
            <div class="radio-group">
                <label>
                    <input type="radio" name="mute" value="false" <%= !user.mute ? "checked" : "" %> />
                    Unmute
                </label>
            </div>
            <button type="submit">Save Preferences</button>
        </form>

        <p class="info">You can update your preferences at any time.</p>
    </div>
</body>
</html>
